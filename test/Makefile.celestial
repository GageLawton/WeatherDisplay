# Mac-only celestial-math test.
#   make -f test/Makefile.celestial
#   ./test_celestial

CXX      := clang++
CXXFLAGS := -std=c++17 -Wall -Wextra -O2 -Iinclude
SRCS     := src/celestial.cpp test/test_celestial.cpp
TARGET   := test_celestial

all: $(TARGET)

$(TARGET): $(SRCS)
	$(CXX) $(CXXFLAGS) $(SRCS) -o $(TARGET)

clean:
	rm -f $(TARGET)

.PHONY: all clean